# @package _global_
# Inherit from base.yaml and override source/target dataset fields
defaults:
  - base.yaml

source:
  dataset: "dukemtmc"
  data_root: "data/raw/dukemtmc"
  num_classes: 702

target:
  dataset: "msmt17"
  data_root: "data/raw/msmt17"
